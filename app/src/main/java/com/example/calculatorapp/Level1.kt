package com.example.calculatorapp

/*
fun main() {

// 숫자와 연산자 입력받는 부분
    print("첫 번째 숫자 입력 : ")
    var number1:Int = readLine()!!.toInt()

    print("두 번째 숫자 입력 : ")
    var number2:Int = readLine()!!.toInt()

    print("연산자 입력 : ")
    var op:String = readLine()!!

// 연산자를 잘못 입력했는지 확인하고 잘못 입력했으면 프로그램 종료
    if(op != "+" && op != "-" && op != "*" && op != "/" && op != "%") {
        println("잘못된 연산자 입력입니다.")
        println("프로그램을 종료합니다.")
        return
    }

// 계산하는 클래스 호출
    var calculator = Calculator(number1, number2, op)
}

class Calculator {

    //변수 선언
    var num1 = 0
    var num2 = 0
    var op2 = ""

    // 변수 정의
    constructor(number1: Int, number2: Int, op:String) {
        num1 = number1
        num2 = number2
        op2 = op

// 여기에서 최종적으로 입력된 연산자에 따라 맞는 연산 코드를 찾아서 연산
        when(op2) {
            "+" -> println("계산 결과 = ${num1 + num2}")
            "-" -> println("계산 결과 = ${num1 - num2}")
            "*" -> println("계산 결과 = ${num1 * num2}")
            "/" -> println("계산 결과 = ${num1 / num2}")
        }
    }
}
*/
